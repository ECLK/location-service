before build the project, you need to specify the server url on `local.properties` file

add this line to `local.properties` file

```properties
    backend.base.url=url
```

example

```properties
    backend.base.url=http://127.0.0.1:8000/
```

